{
  "name": "hugo-update-lastmod",
  "version": "1.0.7",
  "description": "Update Hugo bundle index.md lastmod based on image changes (git log)",
  "type": "module",
  "bin": {
    "hugo-update-lastmod": "bin/hugo-update-lastmod.mjs"
  },
  "scripts": {
    "release:patch": "npm run build:noop && npm version patch && git push && git push --tags && npm publish --access public",
    "release:minor": "npm run build:noop && npm version minor && git push && git push --tags && npm publish --access public",
    "release:major": "npm run build:noop && npm version major && git push && git push --tags && npm publish --access public",
    "build:noop": "echo \"nothing to build\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cnichte/hugo-update-lastmod.git"
  },
  "bugs": {
    "url": "https://github.com/cnichte/hugo-update-lastmod/issues"
  },
  "homepage": "https://github.com/cnichte/hugo-update-lastmod#readme",
  "keywords": [
    "hugo",
    "frontmatter",
    "lastmod",
    "git",
    "images",
    "bundles"
  ],
  "author": "Carsten Nichte",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "glob": "^11.0.3",
    "picocolors": "^1.0.0"
  }
}
